# 取り組んだ課題一覧
- 個人開発
  - Next.js でTodoアプリを作る（CRUD機能のCDを実装）

# 感じたこと
- CRUD機能のCreateからDelete機能を実装する際に、todoのtextだけでは同一のものが消えるので、idを用意。
    - 共通して使いそうな型なので`app/type/todo.ts`を作成し型を作成。こういうロジックで階層を作成していくのかと物語的に理解できた。

# 次やること
- Next.js でTodoアプリを作る（CRUD全て実装、ログイン機能とユーザ名が画面に表示、prisma設定）
- Golang初級（Udemy）

# わかったこと
- ディレクトリごとの役割と違い
```
[Todo]
app/
  tasks/
    page.tsx      ← フロント
  api/
    tasks/
      route.ts    ← API
lib/
  taskStore.ts    ← バックエンド（処理）
```
違い：
① これはユーザーに見せる？ → フロント
② リクエスト受けてる？ → API
③ データ操作してる？ → バックエンド
- trim() 
    - 空白を削除する

# 学習時間
- 3h