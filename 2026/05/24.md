# 取り組んだ課題一覧
- Golang
    - twitterのクローンを作る（ユーザアクティベーション機能）

# 感じたこと
- 現在、プルリク提出のため、プロジェクトをコピーし、ユーザアクティベーション機能の要件を確認。
    - ユーザがURLを踏んだかどうかの状態（true or false）をDBにどう設計するのか。MailHodを使用するので compose.yml の設定。バリデーションの設定（一番難しかったかも）。
```
handler:
JSON形式が正しいか
必須項目があるか
email形式
password形式
tokenが空でないか

service:
email重複
tokenが存在するか
tokenが期限切れでないか
tokenが使用済みでないか
userがすでにactiveでないか
login時にactiveかどうか

DB:
email unique制約
token_hash unique制約
foreign key
NOT NULL
```

# 次やること
- Golang
    - twitterのクローンを作る（ユーザアクティベーション機能）

# わかったこと
- 特になし

# 学習時間
2h