# 取り組んだ課題一覧
- Golang
    - twitterのクローンを作る（サインアップ機能実装）

# 感じたこと
- バックエンドでの実装なので、よく`curl`コマンドで実際にDBへ保存し、期待通りの挙動をするか確認している。
- ３層構造アーキテクチャの実態がつかめた。

# 次やること
- Golang
    - twitterのクローンを作る（サインアップ機能実装） プルリク作成

# わかったこと
- 本当に初歩だが、PostgreSQLの起動の仕方で`docker compose up`か`brew services start`で起動するかで根本的な動きが異なる。
- splcはSQLからGoコードを自動生成するための設定なので、`.yaml`を作成する。

# 学習時間
2h