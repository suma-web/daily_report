# 取り組んだ課題一覧
- JavaScriptでカレンダーを作る
    - カレンダーを表示させるためのコマンド実装
    - 課題完成（PR作成）
- Djangoでtwitterのクローンを作る
    - ログイン機能の実装（django-allauth周りとAbstractUser のモデル作成）
        - [参考サイト](https://zenn.dev/mom/books/4c7430e6a7b321/viewer/76c6b9)

# 感じたこと
### カレンダー課題
- 正直、インポートされるメソッドに`export`を書き忘れていたことに気づき、そこからとんとん拍子で完成。

### twitter課題
- AbstractUser の部分がまだ解説を見ても噛み砕けていない。やりながら復習をしたい。

# わかったこと
- 配列の要素はstring型
```
const arg = process.argv.slice(2);   // オブジェクト型
const command = arg[0];               // string型なのでinteger変換の必要
const option = Number(arg[1]); 
```
- models.pyのFiledの設定では、`null=False`がデフォルト。`null=True`の時だけ記載。
- accounts（CustomUser）のマイグレーションが先。


# 学習時間
5h