# 取り組んだ課題一覧
- JavaScript初級
    - JavaScriptの基本
        - 動画の視聴（~1:05:30）

# 感じたこと
- カジュアル面談でNext.jsまでしっかり取り組む必要が出てきたので、JavaScript系のロードマップ完遂を4月までに取り組む。

# 次やること
- JavaScript初級（ハンズオン含めて）
    - JavaScriptの基本
        - 動画の視聴（1:05:30~）
        - 書籍版 JavaScriptの基本を読む
    - JavaScript入門・完全版
        - 動画の視聴
- Django   
    - 修了の場合、チェックアウト機能の作成。
    - 修正の場合、引き続きカート機能の実装。

# わかったこと
- 変数内でif esleができる
``` 
let userAge = 10
let userType = userAge > 20 ? "adult" : "child";

console.log(userType);
```

- 関係演算子：左側がtrueであれば、右側の関係式が実行されるが、falseの場合は右側は無視。
```
let x = 15;

console.log(x > 5 && x < 20);  //true
console.log(x > 15 && x < 20);  //false
console.log(x > 5 && x < 9);  //false

この性質を利用すると
Console.log(false && ‘a’); が実現可能   //false
```

- Falsyな値（↔︎Truth）
    - false
    - 0（数値ゼロ）
    - null
    - undefined
    - NaN（Not a Number）
    - 空文字列（""）

# 学習時間
 2h