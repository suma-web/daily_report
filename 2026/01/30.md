# 取り組んだ課題一覧
- JavaScript
    - カレンダーを作る（PRまで提出）
    - Todo Listを作る（設計）

# 感じたこと
- PRのコメントが返ってきました。
    - `console.log`の多用はダメ。そもそも`console.log`を使う場面ではなかった。
    - 該当月の日数を配列に入れ、日にち自体のレイアウトを整理する方法を知った。
```
 arrayDays.forEach((day,index) => {
        process.stdout.write(day.toString().padStart(2, " ") + " ");

        }
    });
```

# 次やること
- JavaScript
    - カレンダーを作る（PR再提出）
    - Todo Listを作る（設計をもとにコーデイング）

# わかったこと
- 特になし

# 学習時間
3h