# 取り組んだ課題一覧
- twitterのクローンを作る(Django)
    - `config/template/base.html`でレイアウト調整
    - `login/models.py`の設計書作成
- カレンダーを作る(JavaScript)
    - `Date()`を実際に触る
    - 今月のみ（2026年1月分）表示できるよう設計書作成
    - 今月分カレンダーを表示できるようにコーディング

# 感じたこと
-  `Date()`を実際に触れ、必要な引数がイメージできた。
- 配列に改行を挿入するやり方にすごい時間がかかった。のちにこれは実装には向いていないことがわかった。

# 次やること
- twitterのクローンを作る(Django)
    -  `login/models.py`の設計書をもとにコーディング
- カレンダーを作る(JavaScript)
    - 今月分カレンダーの日にちと曜日がマッチするよう設計する
    - オリジナルコマンドラインの設計書を作成

# わかったこと
- `Date()`はインスタンスを生成してから使用できる。
- 配列に改行の要素を`push`するのではなく、新しく変数を用意し（`例：let str = '';`）、その中に入れ直す。（ここはChatGPTに助言をもらった）

# 学習時間
4h