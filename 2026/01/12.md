# 取り組んだ課題一覧
- Django
    - ECサイトを作る
        - カート実装　修正後merge
        - チェックアウト機能　コーディング前に構想を立てる
- JavaScript
    - JavaScript初級
        - JavaScriptの基本（動画の続き、サイトの閲覧）

# 感じたこと
- カート実装ではモジュールで必要ない`.py`を削除していなかったので整理し、`main`ブランチにマージした。


# 次やること
- Django
    - ECサイトを作る
        - チェックアウト機能
- JavaScript
    - JavaScript初級
        - JavaScriptの基本（サイトの閲覧　続き）

# わかったこと
各種メソッド
```
const message = “こんにちは”

console.log(message.includes(“こん”);  //true
console.log(message.indexOf(“ち”);   //3
console.log(message.replace(“ん”, “に”);   //こににちは
```
Pythonでも見たことがあるような形
```
console.log(“a,b,c,d”.splite(“,”));   // [“a”, “b”, “c”, “d”]
```
時間を取得する
```
const now = new Date();
console.log(now);

引数の扱い方
Date(“2026-01-01”)
Date(2026,1,2)

now.getFullYear();   /年を取得
now.getMonth();  /月を取得
now.getDay();   /日曜が0、土曜が6。数字が出力。
```
配列
```
const fruits = ["りんご", "みかん"];     最後に追加
fruits.push(“パイナップル”, "バナナ");       // ["りんご", "みかん”, “パイナップル”, "バナナ"]

const fruits = ["りんご", "みかん"];      先頭に追加
fruits.push(“パイナップル”, "バナナ");       // [“パイナップル”, "バナナ”, ”りんご", "みかん”]

const fruits = ["りんご", "みかん"];   最後を削除
fruits.pop();       // [”りんご”]

const fruits = ["りんご", "みかん"];   最初を削除
fruits.shift();       // ["みかん"]


fruits.indexOf(“りんご”);       // 0
fruits.indexOf(“柿”);   //存在しないので -1


const products = [
   { id: 1, name: “りんご”},
   { id: 2, name: “みかん”},
   { id: 3, name: “パイナップル”},
]

配列内のデータを探す
const product1 = products.find((product) => product.id == 1);  // { id: 1, name: 'りんご' }が格納
const product1 = products.find((product) => product.id == 4);  // undefined

繰り返し処理（配列の中身を全て見せる）
products.forEach((product) => console.log(product));

同上と結果同じ
let fruits = ['りんご', 'みかん', 'パイナップル'];
for (let fruit of fruits) {
  console.log(fruit);
};   
```

# 学習時間
5h
