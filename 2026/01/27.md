# 取り組んだ課題一覧
- JavaScriptでカレンダーを作る
    - 作成したfunctionメソッドに、年・月を直接入力したらカレンダーが表示できるように実装。
    - オリジナルCLを作成できるようになる。メソッド（`process.argv`）を使用。

# 感じたこと
- 計画通り段階的にカレンダー表示までを実装することができた。（リファクタリングした方が良さそうな感じはするがどうすればいいのか現状ビジョンがない）
- カレンダーを表示するためのファイルとCLのみを記述したファイルに分け、機能の細分化をしたがimportのやり方がうまく行かず。明日はその続きになりそう。

# 次やること
- JavaScriptでカレンダーを作る
    -  PRまで行う。
- DjangoでTwitterのクローンを作る。
    - models.pyの作成。しっかりとテーブルに保存されているか確認。

# わかったこと
- CL作成に必要なメソッド
```
const arg = process.argv.slice(2);
const command = arg[0];
const option = arg[1];
```

# 学習時間
4h