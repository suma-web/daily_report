# 取り組んだ課題一覧
- React上級
    - Reactアプリ開発】を見る（修了）
    - ReactでTrelloクローンを作る（修了）

# 感じたこと
- ドラッグ＆ドロップ（react-beautiful-dnd）ライブラリはUIにおいて非常に重要な要素。それ以外の実装はTodoリストを作るで学習したことなので非常に理解しやすかった。

# 次やること
- Webセキュリティ
    -安全なウェブサイトの作り方を一読
- Golang初級
    - 【プログラミング入門】これからはじめるGo 視聴＆ハンズオン
- TypeScript
    - TypeScriptの基本（動画版を視聴）

# わかったこと
- useIdフックを使用してコンポーネント内で一意なIDを生成し、id属性を付与する。
```
import { useId } from 'react';

const Component =() => {
  const id = useId(); // 一意のIDを生成

  return (
    <div>
            ...
    </div>
  );
}
```

# 学習時間
7h