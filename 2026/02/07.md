# 取り組んだ課題一覧
- JavaScript
    - 【JS】ガチで学びたい人のためのJavaScriptメカニズム（セクション11 MicrotasksとMacroTasks2まで）

# 感じたこと
- `js-Primer`の非同期処理の解像度が上がった。非同期処理の処理工程や処理優先度の理解が深まった。

# 次やること
- JavaScript
    - 【JS】ガチで学びたい人のためのJavaScriptメカニズム（セクション11 途中から）

# わかったこと
- 一連の処理を「スレッド」と言う。
- 非同期処理は一時的にメインスレッドから処理が切り離される。
- 非同期APIはsetTimeout、Promiseなど
- タスクキュー
    - 実行待ちの非同期処理の行列（FIFO）
- 非同期処理のチェーン
    - コールバック関数の中に複数の関数を挿入すると呼び出されるが、可読性が下がる。
        - Promise構文が解決
```
new Promise(function(resolve, reject){
　同期処理
　resolve();   どちらかを記述。
　reject();     どちらかを記述。
}).then(function(){
　非同期処理
}).catch(function(){
　非同期処理
}).finally(function(){
　非同期処理
});
```
```
Promise.all()　→ catchが呼ばれる
Promise.race()
Promise.allSettled()　→ catchが呼ばれない
```
- マイクロタスク（ジョブキュー）　順番が回ってきたら全てのジョブを実行。
- マクロタスク（マクロキュー）　順番が回ってきたら一つずつタスクを実行
    - マイクロタスクのジョブを先に処理。

# 学習時間
2h