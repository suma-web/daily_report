# 取り組んだ課題一覧
- 鬼滅の刃のキャラクター一覧を取得するAPIを叩く（JavaScript）
    - JSONファイルから情報を取得しサイトへ詳細を表示。

# 感じたこと
- `async/await`を使い、`.then`を使わない方法を実践し、可読性の側面を改良できたことを実感した。
- 取得した情報を一度、新規の配列に代入し、`.forEach()`メソッドで取り出せることに驚き。カレンダーでも使用したメソッドが外部から取り込んだ情報でも使用できることは勉強になる。

# 次やること
- 鬼滅の刃のキャラクター一覧を取得するAPIを叩く（JavaScript）
    - `script/`に`load.js`を作成しローディング機能を実装。

# わかったこと
- 選択肢が 3つ以上。表示内容が 条件で変わる。という状況は`fileter(引数,どう引数を使ってフィルターするか)`メソッドを使う。toggleは表示 / 非表示を ON / OFF。2状態が前提。

# 学習時間
- 2.5h