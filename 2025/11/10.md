# 取り組んだ課題一覧
- Python
    - クラスを使った自販機（リポジトリの中にリポジトリを作成する）

# 感じたこと
- 以前にもサブモジュール化に挑戦したが惜しくもできず、今回ようやく克服できたことが嬉しい。
- 今後もつまづくポイントになりそうなので、Qiitaに備忘録として残す。

# 次やること
- ポケモンで学ぶオブジェクト指向を読む
    - 時間があれば、Python で書き直す

# わかったこと
- 今回はサブモジュール化に挑戦した。
    - サブモジュール側のリポジトリの`main`ブランチにREADME.mdがないとそもそも挙動がおかしくなること
    - サブモジュール側のリポジトリの`main`ブランチにファイルがないとそもそもサブモジュール化する際にメインのリポジトリで読み取りエラーになること

# 学習時間
4h