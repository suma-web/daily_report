# 取り組んだ課題一覧
- Django
    - Udemy 修了

# 感じたこと
- Todoリスト基本的に取り組んでいることは変わらないが、`return redirect()`や複数のデータを`POST`する際に工夫がいることなど、体系的なものであることは変わらない。

# 取り組んだ課題一覧
- Django
    - Udemy 総復習
    - Django 公式チュートリアルをやる

# わかったこと
- 複数のデータを送る場合にフォームタグに`enctype="multipart/form-date”`
- 空のデータを送ることができない。`null`を許すなら`models.py`で`null=True, blank=True, (default=0)`を設定

# 学習時間
 2h